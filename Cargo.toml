[package]
name = "vreq"
version = "0.1.0"
edition = "2021"
description = "Generate and sync Python requirements from virtualenvs"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
colored = "2"